title: "Vac Protocol"

markdown: kramdown
theme: minima

collections:
  specs:
    output: true
    permalink: /:path

defaults:
  -
    scope:
      path: ""
    values:
      layout: "spec"

header_pages:
  - waku.md
